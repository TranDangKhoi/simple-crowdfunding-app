import React from "react";

const DashboardRelatedItem = ({ children }) => {
  return (
    <div className="py-1 text-sm bg-white hover:bg-graySoft">{children}</div>
  );
};

export default DashboardRelatedItem;
